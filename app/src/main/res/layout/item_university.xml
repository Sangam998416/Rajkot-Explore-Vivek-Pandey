<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    card_view:cardCornerRadius="16dp"
    card_view:cardElevation="6dp"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- University Image -->
        <ImageView
            android:id="@+id/imgUniversity"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_university_placeholder"
            android:contentDescription="University Image"
            android:background="@color/black" />

        <!-- University Name -->
        <TextView
            android:id="@+id/txtUniversityName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="University Name"
            android:textColor="@android:color/black"
            android:textSize="20sp"
            android:textStyle="bold"
            android:padding="10dp" />

        <!-- University Short Description -->
        <TextView
            android:id="@+id/txtUniversityDesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="A short description about the university goes here."
            android:textColor="@android:color/darker_gray"
            android:textSize="15sp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingBottom="12dp"
            android:maxLines="2"
            android:ellipsize="end" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
